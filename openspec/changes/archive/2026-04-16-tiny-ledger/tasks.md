## 0. Flutter 工程基线

- [x] 0.1 使用 Flutter 创建应用工程（包名、最低 iOS/Android SDK、应用名与图标占位）
- [x] 0.2 建立 `lib/` 分层：`domain/`（纯 Dart）、`data/`（Drift/sqflite 等实现）、`features/`（按功能分模块）、`app/`（`MaterialApp`、主题、路由入口）
- [x] 0.3 锁定状态管理方案（Riverpod 或 Bloc 二选一）并完成最小依赖注入/全局 Provider 基线
- [x] 0.4 配置 `flutter analyze` 与可 CI 运行的测试入口（`dart test` 跑纯领域单测；部分环境 `flutter test` 可能因 shader/impellerc 受限，见 `tiny_ledger/README.md`）

## 1. 领域与数据基础

- [x] 1.1 定义虚拟金额存储策略（整数最小单位）与 Dart 侧展示格式化工具（如 `intl`）
- [x] 1.2 定义核心实体：余额快照、流水条目、储蓄目标、增值演示配置（纯 Dart 模型）
- [x] 1.3 实现本地持久化仓储接口与 Flutter 侧默认实现（**Drift 或 sqflite**，可替换为远程同步）

## 2. 守护与引导

- [x] 2.1 实现首次启动适龄说明与确认流程
- [x] 2.2 在核心路径屏蔽第三方广告与外链营销入口
- [x] 2.3 实现「家长可见摘要」占位页与文案

## 3. 虚拟钱包与流水

- [x] 3.1 实现虚拟余额展示与更新（入账/支出后刷新）
- [x] 3.2 实现支出超额拦截与非负余额校验
- [x] 3.3 实现按时间倒序的流水列表与类型区分展示

## 4. 支出记录与分类

- [x] 4.1 实现支出创建表单（金额、分类、备注）与校验
- [x] 4.2 提供有限儿童友好分类与默认未分类行为
- [x] 4.3 实现入账记录流程并与流水、余额联动

## 5. 储蓄目标

- [x] 5.1 实现目标创建、列表与进度展示组件
- [x] 5.2 实现从余额向目标转入与余额不足拦截
- [x] 5.3 实现目标达成检测与完成反馈（无真实金钱奖励）

## 6. 增值启蒙（模拟）

- [x] 6.1 在增值相关界面展示固定教育标注文案
- [x] 6.2 实现可配置的周期「学习奖励」入账与流水来源标记
- [x] 6.3 确保增值计算仅使用应用内规则，不接入外部金融市场数据

## 7. 动效与现代 UI

- [x] 7.1 建立极简视觉设计令牌（色板、圆角、间距、字体层级）
- [x] 7.2 为记账与目标进度等关键路径接入短动效反馈
- [x] 7.3 实现减弱动效开关并覆盖装饰性动画

## 8. 质量与发布

- [x] 8.1 使用 `dart test` / `flutter test` 为核心领域规则编写单元测试（余额、超额、目标转入、奖励周期）；必要时对关键 Widget 补充 `widget_test`
- [x] 8.2 完成关键路径可用性自测清单（8–10 岁可读性、误触、返回路径）
- [x] 8.3 整理隐私与教育声明文案，准备 **App Store / Google Play** 上架说明与截图指引（Flutter 单工程双端）
